# CONFIG_BCMDHD_SDIO=y is not set
CONFIG_BCMDHD_PCIE=y
CONFIG_MALI_CSF_SUPPORT=y
#屏蔽忽略IOCTL权限
#CONFIG_DRM_IGNORE_IOTCL_PERMIT=n 
#panfrost gem内存管理用到(必选)
CONFIG_DRM_GEM_SHMEM_HELPER=y 
#GPU命令流调度接口，必须打开(必选)
CONFIG_DRM_SCHED=y 
#打开panfrost功能(必选)
CONFIG_DRM_PANFROST=y 
